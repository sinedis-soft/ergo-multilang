import type { CommonDictionary } from "@/dictionaries/types";

export const commonEn: CommonDictionary = {
  actions: { submit: "Submit", sending: "Sending…", retry: "Retry" },
  status: { success: "Success", error: "Error" },
};
